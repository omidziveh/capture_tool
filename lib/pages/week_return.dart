import 'package:flutter/material.dart';

class WeekReturn extends StatelessWidget {
  const WeekReturn({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
